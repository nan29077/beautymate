# 뷰티메이트 리브랜딩 검수 보고서 (2026-08-22)

대상: `E:\프로젝트\뷰티메이트\app` (repo: `nan29077/beautymate`, branch `main`)
검수 방식: 읽기 전용 — 파일 수정·커밋 없음

---

## 종합 판정

**코드 레벨 리브랜딩은 사실상 완료.** `src/`, `prisma/`, 루트 `*.md` / `*.json` 전체에서
`사주 · 점사 · 운세 · 타로 · 상담사 · 점집 · 사주나라 · 사주메이트 · 셀러브릭스 · sajumate · sajunara · sellerbricks`
**검출 0건**. 남은 15건은 전부 `docs/` 하위 과거 SQL·보고서 파일.

다만 **전량 미커밋 상태**이고, 일괄 치환(find & replace)이 남긴 **의미 오류 12건**과
**아바타 자산 소실**이 확인된다.

---

## 1. 커밋 상태 — 조치 필요

| 항목 | 값 |
|------|-----|
| 마지막 커밋 | `ea8b8bf` (2026-08-16) — 사주나라 시절 |
| 추적 파일 변경 | **540건** (수정 412 · 삭제 106 · 스테이징 22) |
| 최근 24시간 수정 | `src`/`prisma`/`docs` 399개 파일 |
| 상태 | 리브랜딩 커밋이 **하나도 없음**, 일부만 index에 스테이징된 혼재 상태 |

또한 `src/app/api/admin/legacy-cleanup/route.ts`, `seed-test-accounts/route.ts`,
`sellers/approve-all/route.ts` 는 **index에서는 삭제(D), 워킹트리에는 파일 존재**(untracked)로 어긋나 있다.
커밋 전에 의도를 정리할 것.

> 권장: `git add -A` 후 `feat: 사주메이트 → 뷰티메이트 리브랜딩` 단일 커밋, 또는 자산/코드/문서로 3분할.

---

## 2. 정상 확인된 항목

| 항목 | 결과 |
|------|------|
| `package.json` | `beautymate`, dev·start 포트 3028 ✅ |
| `ecosystem.config.cjs` | pm2 앱명 `beautymate` ✅ |
| `.env` | `NEXT_PUBLIC_APP_NAME="뷰티메이트"`, URL 3028, DB `mysql://beautymate@127.0.0.1/beautymate` ✅ |
| `.env.example` | 뷰티메이트 기준 ✅ |
| `layout.tsx` 메타데이터 | title/OG/twitter 모두 "뷰티메이트 - 나에게 맞는 뷰티 전문가를 만나는 곳" ✅ |
| 배너 자산 | `public/banners/sajumate` → `public/banners/beautymate` 이동, 참조 4건 전부 실존 ✅ |
| 정적 자산 참조 | 코드 내 리터럴 이미지 경로 12건 **누락 0건** ✅ |
| seed 계정 | `admin@beautymate.com`, `expert1~3@beautymate.kr` ✅ |
| `.next` | 2026-08-22 08:18 재빌드 흔적 ✅ |

---

## 3. 발견된 문제

### 3-1. 일괄 치환이 과거 이력 서술까지 바꿔 자기모순 (9개 파일 12건) — **정확도 문제**

원래 "셀러브릭스/사주메이트 시절"을 가리키던 문장이 "뷰티메이트 시절"로 치환되어,
**현재 브랜드가 자기 자신의 레거시**라고 말하는 상태가 됐다.

| 파일 | 내용 |
|------|------|
| `src/app/(dashboard)/admin/users/page.tsx:18` | `// 뷰티메이트 레거시 역할은 관리자 목록에서 제외` |
| `src/app/(public)/sellers/page.tsx:29` | `// 뷰티메이트 레거시 역할 제외` |
| `src/app/api/admin/legacy-cleanup/route.ts:5,70` | 진단 API 주석 + **사용자 노출 메시지** `"…뷰티메이트 레거시 사용자 및 연관 데이터 삭제 완료"` |
| `src/app/api/live/route.ts:13` | `// (구 뷰티메이트 도메인 잔재 정리 …)` |
| `src/components/shared/BookingContactForm.tsx:6` | `// 뷰티메이트 시절의 배송지 입력…` |
| `src/components/shared/PickSellerButton.tsx:11` | `// 뷰티메이트 시절의 SNS 채널 구독 인증…` |
| `src/lib/defaults.ts:38,119` | `// (뷰티메이트 시절 꿀벌 캐릭터 … 폐기)` |
| `src/lib/roles.ts:10` | `// 뷰티메이트 시절 역할 — 최소 권한으로 강등` |
| `src/lib/siteContent.ts:8,46` | `// 운영 DB Setting 테이블에 뷰티메이트 시절 값이 남아…` |

→ 해당 표현은 **"셀러브릭스"**(레거시 SELLER/BUYER 역할의 출처)로 되돌리는 것이 맞다.
특히 `legacy-cleanup/route.ts:70` 은 관리자 화면에 그대로 출력되는 문구다.

### 3-2. 아바타 자산 소실 — **UI 영향**

- `public/avatars/saju/*.png` **52개 삭제**, 대체 자산은 `public/avatars/beautymate/default.svg` **1개뿐**.
- `src/lib/defaults.ts` 의 모든 아바타 풀이 이 1개로 축소:
  `ADMIN_AVATARS`, `MIDDLE_ADMIN_AVATARS`, `BRAND_AVATARS`, `BUYER_FEMALE/MALE_AVATARS`, `BEAUTYMATE_CUSTOMER_AVATARS` = `["/avatars/beautymate/default.svg"]`
- 결과: `getRandomAvatar()`·해시 기반 아바타가 **전 사용자 동일 이미지** 반환. 깨지지는 않으나 캐릭터 다양성 0.
- 주석은 여전히 `(5종)`, `(6종)`, `(13종)`, `(30종)`, `십이지신 계열 동물 + 한복 + 밤하늘/달·별 일러스트` 로 남아 실제와 불일치.

→ 의도된 임시 조치인지 확인 필요. 뷰티 캐릭터 세트를 새로 넣을 계획이면 주석 수치도 함께 정리.

### 3-3. `prisma/schema.prisma` 주석 치환 오류 (경미)

| 위치 | 현재 | 비고 |
|------|------|------|
| L135 | `// 뷰티 전문가 프로필 (뷰티가/뷰티가)` | 원문 `(점술가/역술가)` → 의미 붕괴, 중복 |
| L67-68 | `뷰티 전문가가입 추천인코드` | `상담사가입` 직역 → `뷰티 전문가 가입` 띄어쓰기 |

### 3-4. 지침 문서(CLAUDE.md / AGENTS.md)가 현실과 불일치 — **AI 협업 리스크**

브랜드 문구만 교체됐고 사실관계는 옛날 그대로다. Codex·Claude가 이 문서를 근거로 잘못 판단할 수 있다.

| 문서 기재 | 실제 |
|-----------|------|
| `DATABASE_URL`은 **실서버 운영 RDS 직결** | 로컬 `127.0.0.1:3306/beautymate` |
| 개발 서버 `localhost:3000` | **3028** |
| Role System **4 roles** (`SELLER`/`BUYER`/`BRAND_ADMIN`) | `SUPER_ADMIN`/`CONSULTANT`/`CUSTOMER` + 레거시 5종 |
| API route **52개** | **192개** (page.tsx 95개) |
| 데이터 흐름 "브랜드→셀러 공동구매" | 뷰티 전문가 예약·상담 중심 |

`README.md` 테스트 계정표도 `seller1@beautymate.com` / `brand1@beautymate.com` 로 적혀 있으나
실제 `prisma/seed.ts` 는 `expert1~3@beautymate.kr` 를 생성한다.

### 3-5. 최상위 폴더의 옛 사본 방치

`E:\프로젝트\뷰티메이트\` 최상위의 `src` · `prisma` · `package.json`(`sajunara`, 포트 3023) · `CLAUDE.md`(사주메이트)는
**리브랜딩 대상에서 제외된 사주메이트 시절 복사본**이며 2026-08-20 이후 수정 0건.
git 추적 대상도 아니다. 혼동 방지를 위해 삭제 또는 `_archive_사주메이트/` 로 격리 권장.

### 3-6. 기타 경미

- `docs/` 잔여 `사주` 15건: `DDL_CONSULTING.sql`, `DUMMY_DATA.sql`(더미 상품명), `구현완료보고서-2026-08-13.md`, `요구사항분석-2026-08-12.md`. 과거 기록이라 방치해도 무방하나, **과거 보고서 본문은 브랜드가 뷰티메이트로 치환되어 사실과 어긋난 이력 문서**가 됐다.
- `꿀벌` 언급 6개 파일, `src/app/icon_bee_*.png` 등 셀러브릭스 시절 자산 잔존.

---

## 4. 미검증 항목

- **`npx tsc --noEmit` 전체 타입체크**: 원격 셸 45초 제한으로 완주 불가(40초 시점 미완). 로컬에서 실행 필요.
- `npm run build` 프로덕션 빌드, 런타임 화면 확인.

---

## 5. 권장 조치 순서

1. 로컬에서 `npx tsc --noEmit` + `npm run build` 통과 확인
2. 3-1 자기모순 주석 12건을 "셀러브릭스"로 정정 (특히 `legacy-cleanup/route.ts:70` 노출 문구)
3. 3-3 schema 주석 2건 수정
4. 아바타 자산 방침 결정 (신규 세트 투입 or 1종 유지 + 주석 정리)
5. `CLAUDE.md` / `AGENTS.md` / `README.md` 를 현재 사실(포트 3028, 로컬 DB, CONSULTANT/CUSTOMER, API 192개, expert 계정)로 갱신
6. 스테이징 혼재 정리 후 리브랜딩 커밋 & 푸시
7. 최상위 사주메이트 사본 격리·삭제
